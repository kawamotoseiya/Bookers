<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
  	<h2><%= @book_image.errors.count %>件のエラーが発生しました。</h2>
  	<% @book.errors.full_messages.each do |message| %>
  	<li><%= message %></li>
   <% end %>
  <% end %>
<label>Title</label><br>
<%= f.text_field :title %><br>
<label>Opinion</label><br>
<%= f.text_area :body %><br>
<%= f.submit "Create Book" %>
<% end %>
